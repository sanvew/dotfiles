- defaults:
    link:
      create: true
      relink: true

- link:
    ~/:
      create: true
      glob: true
      force: true
      path: './bash/**'
      prefix: '.'
      exclude: [ '**/bash.conf.yaml' ]

